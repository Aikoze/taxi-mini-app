// Configuration pour localtunnel
export default {
  // Port sur lequel votre application Vite est en cours d'exécution
  port: 5173,
  
  // Sous-domaine personnalisé (optionnel)
  // Note: le sous-domaine demandé peut ne pas être disponible
  subdomain: 'taxi-mini-app',
  
  // Hôte localtunnel (optionnel)
  // host: 'https://localtunnel.me',
  
  // Options supplémentaires
  local_host: 'localhost',
  allow_invalid_cert: true
};
